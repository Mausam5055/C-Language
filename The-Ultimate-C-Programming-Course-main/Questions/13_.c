//✅ Q13. Increment an integer using ++ and show result before and after.
#include <stdio.h>

int main() {
    int x = 5;

    printf("Before increment: %d\n", x);
    x++;  // or use ++x;
    printf("After increment: %d\n", x);

    return 0;
}
// x++  means x = x+1