// Q2. Write a program to calculate and print the result of this expression:
#include <stdio.h>

int main() {
    int a = 2, b = 3, c = 4;
    int result;

    result = (a + b) * c;

    printf("Result of (a + b) * c is: %d\n", result);

    return 0;
}

